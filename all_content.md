# public/index.html
- ClearFlow | אוטומציה עסקית שעושה סדר
- {
        "@context": "https://schema.org",
        "@type": "ProfessionalService",
        "name": "ClearFlow",
        "url": "https://clearflow.co.il/",
        "logo": "https://clearflow.co.il/logo.png",
        "image": "https://clearflow.co.il/og-image.jpg",
        "description": "סוכנות אוטומציה עסקית בישראל. אנו מתמחים בהקמת תהליכי אוטומציה, ניווט נתונים, אינטגרציה בין מערכות (Make.com, Zapier) והטמעת מערכות CRM.",
        "address": {
            "@type": "PostalAddress",
            "addressCountry": "IL"
        },
        "telephone": "+972522296269",
        "email": "shai@clearflow.co.il",
        "founder": {
            "@type": "Person",
            "name": "Shai Israel"
        },
        "areaServed": "IL",
        "priceRange": "$$",
        "knowsAbout": ["Business Automation", "Make.com", "Zapier", "CRM", "Data Integration"]
    }
- דלג לתוכן המרכזי
- פתרונות נגישות לאתרי אינטרנט לפי התקן הישראלי 5568
- הנגשת
            אתרים
- (נפתח בחלון חדש)
- אנו משתמשים בעוגיות (Cookies) כדי להעניק לך את החוויה הטובה ביותר. בהמשך הגלישה, הנך מסכים/ה לשימוש בהן.
                לקריאה נוספת:
- מדיניות הפרטיות
- שלנו.
- הבנתי תודה
- מדיניות פרטיות
- מדיניות פרטיות - ClearFlow
- חברת ClearFlow ('אנחנו', 'אותנו' או 'שלנו') מכבדת את פרטיותך ומחויבת להגנה על מידע אישי שאתה משתף איתנו.
                מדיניות זו מסבירה כיצד אנו אוספים ומשתמשים במידע.
- 1. המידע שאנו אוספים:
- אנו עשויים לאסוף שם מלא, כתובת דוא״ל, מספר טלפון, ומידע על השימוש
                באתר הנאסף אוטומטית באמצעות עוגיות (Cookies).
- 2. מטרות השימוש במידע:
- מתן שירותים, יצירת קשר, תמיכה, ושליחת חומרים שיווקיים (בכפוף
                להסכמתך).
- 3. שיתוף מידע:
- אנו לא נמכור או נשכיר מידע אישי לצדדים שלישיים. מידע עשוי להיות מועבר
                לספקי שירותים חיצוניים לצורך תפעול האתר בלבד, או לרשויות החוק אם נדרש.
- 4. אבטחת מידע:
- אנו נוקטים באמצעי אבטחה מתקדמים להגנה על המידע האישי.
- 5. זכויות המשתמש:
- יש לך זכות לעיין במידע, לבקש את תיקונו או מחיקתו.
- 6. יצירת קשר:
- לשאלות, ניתן לפנות אלינו בטלפון 052-229-6269 או בדוא״ל
                shai@clearflow.co.il.
- הצהרת נגישות
- הצהרת נגישות - ClearFlow
- אנו ב-ClearFlow מייחסים חשיבות עליונה להנגשת אתר האינטרנט שלנו. אתר זה הונגש בהתאם להמלצות התקן הישראלי
                (ת״י 5568) לנגישות תכנים באינטרנט ברמת AA, וכן עומד בהנחיות WCAG 2.0.
- פירוט התאמות:
- האתר כולל תמיכה בניווט מקלדת, התאמה לקוראי מסך, ואפשרות להגדלת גופנים
                ושינוי ניגודיות באמצעות תוסף הנגישות (Tabnav) המותקן באתר.
- סייגים:
- למרות מאמצינו להנגיש את כלל דפי האתר, ייתכן ויתגלו חלקים שטרם הונגשו
                במלואם.
- פרטי רכז נגישות:
- במידה ונתקלתם בבעיית נגישות, נשמח לקבל משוב ולתקן זאת בהקדם.
- שם הרכז: שי ישראל
- טלפון: 052-229-6269
- דוא״ל: shai@clearflow.co.il
- בית
- שירותים
- דפי נחיתה
- אוטומציות מתקדמות
- מערכות CRM
- הדרכה והטמעה
- יצירת קשר
- אוטומציה עסקית ש
- עושה סדר
- פחות עבודה ידנית, יותר שליטה. אנחנו רותמים סוכני בינה מלאכותית ואוטומציה כדי לחסוך לכם שעות, למנוע מלידים ליפול, ולהחזיר לכם את השליטה המלאה על התפעול.
- לקביעת שיחת ייעוץ ללא עלות
- שליחה
- אני מאשר/ת שקראתי את
- תנאי
                                            מדיניות הפרטיות
- ואת קבלת הדיוור.
- תודה!
- הפרטים התקבלו בהצלחה.
- סדר וארגון
- חיסכון בשעות עבודה
- שליטה מלאה
- מעקב אחרי כל ליד
- אפס התעסקות
- מינימום טעויות אנוש
- העסק צומח, אבל התפעול תוקע
                        אתכם מאחור?
- המצב היום
                            (הבלאגן)
- לידים חמים נופלים בין הכיסאות.
- עובדים שורפים שעות על העתק-הדבק.
- תהליכי קליטת לקוחות מסורבלים ואיטיים.
- הפתרון (אחרי
                            אוטומציה)
- מעקב אוטומטי אחר כל פנייה וליד.
- סנכרון נתונים שקוף בין כל המערכות.
- תהליכי Onboarding מהירים ואוטומטיים.
- השירותים שלנו
- דפי נחיתה שממירים יותר
- עיצוב ובניית דפי נחיתה מהירים ומתקדמים, שבנויים פסיכולוגית וטכנית להמיר גולשים ללקוחות.
- קרא עוד
- אוטומציה לחיסכון של שעות
- חיבור חכם בין כל המערכות שלכם (Make/Zapier) כדי למנוע פעולות כפולות ולחסוך שעות של עבודה
                            ידנית.
- קרא עוד
- מרכז ניהול לקוחות (CRM) מאורגן
- אפיון, הקמה והטמעה של מערכות ניהול לקוחות (CRM) שמותאמות בדיוק למידות של העסק שלכם.
- קרא עוד
- ליווי והדרכת הצוות שלכם
- אנחנו לא משאירים אתכם באוויר. העברת מקל מסודרת והדרכה מקיפה לצוות כדי שתנהלו את המערכת
                            בעצמאות.
- קרא עוד
- מתהליך מסורבל למכונה משומנת
                        ב-3 שלבים:
- 1. מיפוי חסר פשרות
- שיחת עומק אסטרטגית שבה מפרקים את צווארי הבקבוק, מזהים איפה הכסף בורח, ובונים תוכנית פעולה
                            תכל'ס.
- 2. ארכיטקטורה ואוטומציה
- בניית תהליכים שקופים. קליטת לידים ל-CRM, הפקת חשבוניות, וסנכרון מלא בין מחלקות העסק.
- 3. הדרכה והעברת מקל
- בסיום ההטמעה, הצוות שלכם מקבל הדרכה מקיפה וליווי צמוד כדי שינהלו את המערכת בעצמאות מלאה.
- מדריך חינם
- האם העסק שלכם מוכן לאוטומציה?
- ודאו שאינכם משאירים כסף על השולחן — הורידו את הצ'ק-ליסט המקצועי שלנו שיעזור לכם לזהות היכן בורחות השעות (וההכנסות) בעסק.
- 7 נורות האזהרה התפעוליות שמעכבות את העסק שלכם
- האמת העירומה: למה אסור לעולם לאטמט תהליך שבור?
- חוק ה-80/20 של האוטומציה המודרנית ליצירת צמיחה
- לקבלת הצ'ק-ליסט למייל
- שלחו לי את המדריך
- אני מסכים/ה ל
- מדיניות
                                            הפרטיות
- ולקבלת דיוור (אפשר להסיר תמיד).
- מעולה!
- הצ'ק-ליסט בדרך אליכם!
- בדקו את תיבת הדואר שלכם.
- שאלות נפוצות
- כמה זמן לוקח להקים אוטומציה?
- זה תלוי במורכבות, אבל תהליך ממוצע לוקח בין שבועיים לחודש. אנחנו מתחילים במיפוי מהיר ותוך ימים ספורים כבר רואים את האוטומציות הראשונות רצות.
- האם זה מתאים גם לעסקים קטנים?
- בהחלט. דווקא בעסקים קטנים, שבהם כל דקה חשובה ואין כוח אדם מיותר, האוטומציה היא "מכפיל כוח" קריטי שמאפשר לכם לצמוח בלי להחתים עוד פקידים.
- כמה זה עולה?
- כל פרויקט שונה, אז אנחנו לא עובדים עם חבילות מדף. אחרי שיחת המיפוי הראשונית (שהיא חינם), אנחנו מגישים הצעת מחיר מפורטת ושקופה שמבוססת על הצרכים המדויקים שלכם — בלי הפתעות.
- האם אני אצטרך לדעת לתכנת?
- ממש לא. כל המערכות שאנחנו בונים הן No-Code או Low-Code. בסוף התהליך תקבלו הדרכה שתאפשר לכם להבין בדיוק מה קורה "מתחת למכסה המנוע" בלי לכתוב שורת קוד אחת.
- האם אתם עובדים עם מערכות CRM קיימות?
- כן, אנחנו מתמחים בחיבור ומקסום יכולות של מערכות כמו Monday, HubSpot, Pipedrive ועוד. אם המערכת שלכם פתוחה לחיבורים (API), אנחנו נדע להפוך אותה לאוטומטית.
- מוכנים להתחיל לחסוך בזמן?
- השאירו פרטים לשיחת מיפוי קצרה (ללא עלות), בה נזהה איפה הכסף והזמן שלכם בורחים.
- אני מאשר/ת שקראתי את
- מדיניות הפרטיות
- ואת קבלת הדיוור.
- בואו נדבר תכל'ס
- תודה רבה!
- הפרטים התקבלו. נחזור אליכם בהקדם לתיאום פגישה.
- © 2024 ClearFlow Automation. כל הזכויות שמורות.
- פרטיות
- הצהרת נגישות
- // Webhook Submission Logic
        async function submitToWebhook(payload, submitBtn, errorEl) {
            const webhookUrl = 'https://hook.eu1.make.com/p3evkp5wlspqvo3g1ln8qyxanf23h7as';
            const originalText = submitBtn.innerText;
            try {
                submitBtn.disabled = true;
                submitBtn.innerText = 'שולח...';
                if (errorEl) errorEl.classList.add('hidden');
                const response = await fetch(webhookUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                if (response.ok) return true;
                else throw new Error('Server error');
            } catch (error) {
                console.error('Submission error:', error);
                if (errorEl) {
                    errorEl.innerText = 'חלה שגיאה בשליחה. אנא נסו שנית או פנו אלינו בוואטסאפ.';
                    errorEl.classList.remove('hidden');
                }
                submitBtn.disabled = false;
                submitBtn.innerText = originalText;
                return false;
            }
        }

        // Header Scroll Interaction
        const header = document.getElementById('mainHeader');
        if (header) {
            window.addEventListener('scroll', () => {
                if (window.scrollY > 20) {
                    header.classList.remove('bg-white/40', 'border-transparent', 'py-4');
                    header.classList.add('bg-[#11223A]', 'border-gray-100', 'shadow-md', 'py-3');
                } else {
                    header.classList.add('bg-white/40', 'border-transparent', 'py-4');
                    header.classList.remove('bg-[#11223A]', 'border-gray-100', 'shadow-md', 'py-3');
                }
            });
        }

        // Shared Form Validator
        function validateForm(name, phone, privacyChecked) {
            if (!name || !phone) return 'אנא מלאו את כל השדות.';
            if (!privacyChecked) return 'יש לאשר את מדיניות הפרטיות.';
            return null;
        }

        // Hero Form Logic
        const heroForm = document.querySelector('section:first-of-type form');
        if (heroForm) {
            heroForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = heroForm.querySelector('input[name="name"]').value.trim();
                const phoneVal = heroForm.querySelector('input[name="phone"]').value.trim();
                const privacyChecked = heroForm.querySelector('#privacy-hero').checked;
                const errorEl = heroForm.querySelector('.hero-error-message');
                const errMsg = validateForm(nameVal, phoneVal, privacyChecked);
                if (errMsg) {
                    errorEl.innerText = errMsg;
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = heroForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, phone: phoneVal, source: sourceVal }, heroForm.querySelector('button'), errorEl);
                if (success) {
                    document.getElementById('heroFormInputs').classList.add('hidden');
                    document.getElementById('heroFormSuccess').classList.remove('hidden');
                }
            });
        }

        // Footer Form Logic
        const footerForm = document.getElementById('footerForm');
        if (footerForm) {
            footerForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = footerForm.querySelector('input[name="name"]').value.trim();
                const phoneVal = footerForm.querySelector('input[name="phone"]').value.trim();
                const emailVal = footerForm.querySelector('input[name="email"]').value.trim();
                const privacyChecked = document.getElementById('privacy-footer').checked;
                const errorEl = footerForm.querySelector('.footer-error-message');
                if (!nameVal || !phoneVal || !emailVal) {
                    errorEl.innerText = 'אנא מלאו שדות חובה.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                if (!privacyChecked) {
                    errorEl.innerText = 'יש לאשר את מדיניות הפרטיות.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = footerForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, phone: phoneVal, email: emailVal, source: sourceVal }, footerForm.querySelector('button'), errorEl);
                if (success) {
                    footerForm.classList.add('hidden');
                    document.getElementById('footerSuccess').classList.remove('hidden');
                }
            });
        }

        // Lead Magnet Form Logic
        const leadForm = document.getElementById('leadMagnetForm');
        if (leadForm) {
            leadForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = leadForm.querySelector('input[name="fullName"]').value.trim();
                const emailVal = leadForm.querySelector('input[name="email"]').value.trim();
                const privacyChecked = leadForm.querySelector('input[name="privacyAgreement"]').checked;
                const errorEl = leadForm.querySelector('.lead-error-message');
                if (!nameVal || !emailVal) {
                    errorEl.innerText = 'אנא מלאו שם ודוא״ל.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                if (!privacyChecked) {
                    errorEl.innerText = 'יש לאשר את מדיניות הפרטיות.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = leadForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, email: emailVal, source: sourceVal }, leadForm.querySelector('button'), errorEl);
                if (success) {
                    leadForm.classList.add('hidden');
                    document.getElementById('leadMagnetSuccess').classList.remove('hidden');
                }
            });
        }

        // Reveal Animations
        document.addEventListener("DOMContentLoaded", () => {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('is-revealed');
                    }
                });
            }, { threshold: 0.15 });
            document.querySelectorAll('.reveal-element').forEach(el => observer.observe(el));
        });
# public/services/automations.html
- אוטומציה לחיסכון של שעות | ClearFlow
- {
        "@context": "https://schema.org",
        "@type": "ProfessionalService",
        "name": "ClearFlow",
        "url": "https://clearflow.co.il/",
        "logo": "https://clearflow.co.il/logo.png",
        "image": "https://clearflow.co.il/og-image.jpg",
        "description": "סוכנות אוטומציה עסקית בישראל. אנו מתמחים בהקמת תהליכי אוטומציה, ניווט נתונים, אינטגרציה בין מערכות (Make.com, Zapier) והטמעת מערכות CRM.",
        "address": {
            "@type": "PostalAddress",
            "addressCountry": "IL"
        },
        "telephone": "+972522296269",
        "email": "shai@clearflow.co.il",
        "founder": {
            "@type": "Person",
            "name": "Shai Israel"
        },
        "areaServed": "IL",
        "priceRange": "$$",
        "knowsAbout": ["Business Automation", "Make.com", "Zapier", "CRM", "Data Integration"]
    }
- דלג לתוכן המרכזי
- פתרונות נגישות לאתרי אינטרנט לפי התקן הישראלי 5568
- הנגשת
            אתרים
- (נפתח בחלון חדש)
- אנו משתמשים בעוגיות (Cookies) כדי להעניק לך את החוויה הטובה ביותר. בהמשך הגלישה, הנך מסכים/ה לשימוש בהן.
                לקריאה נוספת:
- מדיניות הפרטיות
- שלנו.
- הבנתי תודה
- מדיניות פרטיות
- מדיניות פרטיות - ClearFlow
- חברת ClearFlow ('אנחנו', 'אותנו' או 'שלנו') מכבדת את פרטיותך ומחויבת להגנה על מידע אישי שאתה משתף איתנו.
                מדיניות זו מסבירה כיצד אנו אוספים ומשתמשים במידע.
- 1. המידע שאנו אוספים:
- אנו עשויים לאסוף שם מלא, כתובת דוא״ל, מספר טלפון, ומידע על השימוש
                באתר הנאסף אוטומטית באמצעות עוגיות (Cookies).
- 2. מטרות השימוש במידע:
- מתן שירותים, יצירת קשר, תמיכה, ושליחת חומרים שיווקיים (בכפוף
                להסכמתך).
- 3. שיתוף מידע:
- אנו לא נמכור או נשכיר מידע אישי לצדדים שלישיים. מידע עשוי להיות מועבר
                לספקי שירותים חיצוניים לצורך תפעול האתר בלבד, או לרשויות החוק אם נדרש.
- 4. אבטחת מידע:
- אנו נוקטים באמצעי אבטחה מתקדמים להגנה על המידע האישי.
- 5. זכויות המשתמש:
- יש לך זכות לעיין במידע, לבקש את תיקונו או מחיקתו.
- 6. יצירת קשר:
- לשאלות, ניתן לפנות אלינו בטלפון 052-229-6269 או בדוא״ל
                shai@clearflow.co.il.
- הצהרת נגישות
- הצהרת נגישות - ClearFlow
- אנו ב-ClearFlow מייחסים חשיבות עליונה להנגשת אתר האינטרנט שלנו. אתר זה הונגש בהתאם להמלצות התקן הישראלי
                (ת״י 5568) לנגישות תכנים באינטרנט ברמת AA, וכן עומד בהנחיות WCAG 2.0.
- פירוט התאמות:
- האתר כולל תמיכה בניווט מקלדת, התאמה לקוראי מסך, ואפשרות להגדלת גופנים
                ושינוי ניגודיות באמצעות תוסף הנגישות (Tabnav) המותקן באתר.
- סייגים:
- למרות מאמצינו להנגיש את כלל דפי האתר, ייתכן ויתגלו חלקים שטרם הונגשו
                במלואם.
- פרטי רכז נגישות:
- במידה ונתקלתם בבעיית נגישות, נשמח לקבל משוב ולתקן זאת בהקדם.
- שם הרכז: שי ישראל
- טלפון: 052-229-6269
- דוא״ל: shai@clearflow.co.il
- בית
- שירותים
- דפי נחיתה
- אוטומציות מתקדמות
- מערכות CRM
- הדרכה והטמעה
- יצירת קשר
- חזרה לעמוד הראשי
- אוטומציה עסקית: לעבוד חכם, לא קשה
- בתור עצמאיים או מנהלי עסקים קטנים, הזמן הוא המשאב היקר ביותר שאתם שורפים. אנחנו משלבים סוכני בינה מלאכותית (AI) יחד עם מערכים תפעוליים שקופים, כדי להפוך פעולות ידניות ורוטיניות בעסק למערך אוטומטי, מהיר ונטול תקלות.
- 4 דקות קריאה
- הבעיה השקופה: 'מס-התפעול' של העסק שלכם
- ככל שעסק צומח, כמות הפעולות הטכניות והחזרתיות מזנקת. הצוות שלכם מתחיל לתפקד כמו 'רובוטים אנושיים': מעתיקים נתונים מטופס ליצירת קשר לקובץ אקסל, שולחים הודעות גנריות בסוף כל שיחה, מנסים להעביר מידע למחלקת משאבי אנוש, ושוכחים משימות חשובות בדרך. התהליכים האלו אולי נראים קטנים וטבעיים ביום-יום, אבל במצטבר הם יוצרים צוואר בקבוק שחונק את רווחיות העסק, גורם לטעויות אנוש הרסניות מול הלקוח, ושורף שעות עבודה יקרות של עובדים שצריכים להתעסק בפיתוח עסקי במקום בהעתק-הדבק.
- מיקום לאיור / תרשים (Placeholder)
- איך פותרים את הכאוס? מעבר לעבודה חכמה
- הפתרון לא טמון בגיוס עוד כוח אדם או ביצירת עוד נהלים מסורבלים, אלא בבניית תשתית אוטומטית שבאמת 'מחברת את הנקודות'. אוטומציה עסקית עובדת מאחורי הקלעים וגורמת לתוכנות שלכם (כמו האימייל, ה-CRM במשרד, ומערכות התשלום) 'לדבר' אחת עם השנייה במקומכם. תהליך נכון מתחיל במיפוי קפדני של כל פעולה שחוזרת על עצמה יותר מ-3 פעמים ביום שצוות שלם מבצע. לאחר מכן, תופרים לוגיקה חכמה שגורמת למערכת לבצע את הפעולות הללו בצורה אוטומטית וחלקה מיד כשמתרחש אירוע מסוים. מודלים של אוטומציות תקשורת חכמות (כדוגמת אלה שהוטמעו בחברת foodora) הוכיחו כי תזמון אוטומטי יכול לחתוך את נטישת הלקוחות ב-26%.
- מיקום לאיור / תרשים (Placeholder)
- בשטח: אוטומציה בפעולה מהירה
- לקוח פוטנציאלי משאיר פרטים בקמפיין הרשת שלכם. בפחות מזמן נשימה (2 שניות), המערכת שהקמנו תבצע אוטומטית: תפתח לו כרטיס חדש ב-CRM, **תשתמש בסוכן בינה מלאכותית לקרוא ולנתח את צרכי הלקוח**, תשלח לו הודעת תודה אישית ממוקדת ואנושית בוואטסאפ, ותתריע לאיש המכירות הפנוי ביותר עם תקציר מסודר של הכל. כל התפעול הזה קורה לבד, ללא מגע יד אדם.
- השורה התחתונה
- חיסכון מיידי של עשרות שעות שבועיות – משימות שעוברות לטיפול על ידי אלגוריתם אוטומטי במקום לנהל צוות.
- אפס טעויות אנוש בשגרה המשרדית. הכל מתעדכן ומסונכרן מעצמו.
- חווית לקוח חסרת תחרות עם מענה חכם ומהיר יותר מכל מתחרה אנושי בשוק.
- בואו נחסוך לכם 10-15 שעות עבודה בשבוע.
- ספרו לנו איזו משימה משרדית גוזלת לכם הכי הרבה שעות ביום, ונדאג להעלים אותה עד מחר בבוקר.
- לייעוץ ותכנון חינם
- © 2024 ClearFlow Automation. כל הזכויות שמורות.
- פרטיות
- הצהרת נגישות
- // Webhook Submission Logic
        async function submitToWebhook(payload, submitBtn, errorEl) {
            const webhookUrl = 'https://hook.eu1.make.com/p3evkp5wlspqvo3g1ln8qyxanf23h7as';
            const originalText = submitBtn.innerText;
            try {
                submitBtn.disabled = true;
                submitBtn.innerText = 'שולח...';
                if (errorEl) errorEl.classList.add('hidden');
                const response = await fetch(webhookUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                if (response.ok) return true;
                else throw new Error('Server error');
            } catch (error) {
                console.error('Submission error:', error);
                if (errorEl) {
                    errorEl.innerText = 'חלה שגיאה בשליחה. אנא נסו שנית או פנו אלינו בוואטסאפ.';
                    errorEl.classList.remove('hidden');
                }
                submitBtn.disabled = false;
                submitBtn.innerText = originalText;
                return false;
            }
        }

        // Header Scroll Interaction
        const header = document.getElementById('mainHeader');
        if (header) {
            window.addEventListener('scroll', () => {
                if (window.scrollY > 20) {
                    header.classList.remove('shadow-lg', 'py-4');
                    header.classList.add('shadow-md', 'py-3');
                } else {
                    header.classList.add('shadow-lg', 'py-4');
                    header.classList.remove('shadow-md', 'py-3');
                }
            });
        }

        // Shared Form Validator
        function validateForm(name, phone, privacyChecked) {
            if (!name || !phone) return 'אנא מלאו את כל השדות.';
            if (!privacyChecked) return 'יש לאשר את מדיניות הפרטיות.';
            return null;
        }

        // Hero Form Logic
        const heroForm = document.querySelector('section:first-of-type form');
        if (heroForm) {
            heroForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = heroForm.querySelector('input[name="name"]').value.trim();
                const phoneVal = heroForm.querySelector('input[name="phone"]').value.trim();
                const privacyChecked = heroForm.querySelector('#privacy-hero').checked;
                const errorEl = heroForm.querySelector('.hero-error-message');
                const errMsg = validateForm(nameVal, phoneVal, privacyChecked);
                if (errMsg) {
                    errorEl.innerText = errMsg;
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = heroForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, phone: phoneVal, source: sourceVal }, heroForm.querySelector('button'), errorEl);
                if (success) {
                    document.getElementById('heroFormInputs').classList.add('hidden');
                    document.getElementById('heroFormSuccess').classList.remove('hidden');
                }
            });
        }

        // Footer Form Logic
        const footerForm = document.getElementById('footerForm');
        if (footerForm) {
            footerForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = footerForm.querySelector('input[name="name"]').value.trim();
                const phoneVal = footerForm.querySelector('input[name="phone"]').value.trim();
                const emailVal = footerForm.querySelector('input[name="email"]').value.trim();
                const privacyChecked = document.getElementById('privacy-footer').checked;
                const errorEl = footerForm.querySelector('.footer-error-message');
                if (!nameVal || !phoneVal || !emailVal) {
                    errorEl.innerText = 'אנא מלאו שדות חובה.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                if (!privacyChecked) {
                    errorEl.innerText = 'יש לאשר את מדיניות הפרטיות.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = footerForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, phone: phoneVal, email: emailVal, source: sourceVal }, footerForm.querySelector('button'), errorEl);
                if (success) {
                    footerForm.classList.add('hidden');
                    document.getElementById('footerSuccess').classList.remove('hidden');
                }
            });
        }

        // Lead Magnet Form Logic
        const leadForm = document.getElementById('leadMagnetForm');
        if (leadForm) {
            leadForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = leadForm.querySelector('input[name="fullName"]').value.trim();
                const emailVal = leadForm.querySelector('input[name="email"]').value.trim();
                const privacyChecked = leadForm.querySelector('input[name="privacyAgreement"]').checked;
                const errorEl = leadForm.querySelector('.lead-error-message');
                if (!nameVal || !emailVal) {
                    errorEl.innerText = 'אנא מלאו שם ודוא״ל.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                if (!privacyChecked) {
                    errorEl.innerText = 'יש לאשר את מדיניות הפרטיות.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = leadForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, email: emailVal, source: sourceVal }, leadForm.querySelector('button'), errorEl);
                if (success) {
                    leadForm.classList.add('hidden');
                    document.getElementById('leadMagnetSuccess').classList.remove('hidden');
                }
            });
        }

        // Reveal Animations
        document.addEventListener("DOMContentLoaded", () => {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('is-revealed');
                    }
                });
            }, { threshold: 0.15 });
            document.querySelectorAll('.reveal-element').forEach(el => observer.observe(el));
        });
# public/services/crm-systems.html
- מרכז ניהול לקוחות (CRM) מאורגן | ClearFlow
- {
        "@context": "https://schema.org",
        "@type": "ProfessionalService",
        "name": "ClearFlow",
        "url": "https://clearflow.co.il/",
        "logo": "https://clearflow.co.il/logo.png",
        "image": "https://clearflow.co.il/og-image.jpg",
        "description": "סוכנות אוטומציה עסקית בישראל. אנו מתמחים בהקמת תהליכי אוטומציה, ניווט נתונים, אינטגרציה בין מערכות (Make.com, Zapier) והטמעת מערכות CRM.",
        "address": {
            "@type": "PostalAddress",
            "addressCountry": "IL"
        },
        "telephone": "+972522296269",
        "email": "shai@clearflow.co.il",
        "founder": {
            "@type": "Person",
            "name": "Shai Israel"
        },
        "areaServed": "IL",
        "priceRange": "$$",
        "knowsAbout": ["Business Automation", "Make.com", "Zapier", "CRM", "Data Integration"]
    }
- דלג לתוכן המרכזי
- פתרונות נגישות לאתרי אינטרנט לפי התקן הישראלי 5568
- הנגשת
            אתרים
- (נפתח בחלון חדש)
- אנו משתמשים בעוגיות (Cookies) כדי להעניק לך את החוויה הטובה ביותר. בהמשך הגלישה, הנך מסכים/ה לשימוש בהן.
                לקריאה נוספת:
- מדיניות הפרטיות
- שלנו.
- הבנתי תודה
- מדיניות פרטיות
- מדיניות פרטיות - ClearFlow
- חברת ClearFlow ('אנחנו', 'אותנו' או 'שלנו') מכבדת את פרטיותך ומחויבת להגנה על מידע אישי שאתה משתף איתנו.
                מדיניות זו מסבירה כיצד אנו אוספים ומשתמשים במידע.
- 1. המידע שאנו אוספים:
- אנו עשויים לאסוף שם מלא, כתובת דוא״ל, מספר טלפון, ומידע על השימוש
                באתר הנאסף אוטומטית באמצעות עוגיות (Cookies).
- 2. מטרות השימוש במידע:
- מתן שירותים, יצירת קשר, תמיכה, ושליחת חומרים שיווקיים (בכפוף
                להסכמתך).
- 3. שיתוף מידע:
- אנו לא נמכור או נשכיר מידע אישי לצדדים שלישיים. מידע עשוי להיות מועבר
                לספקי שירותים חיצוניים לצורך תפעול האתר בלבד, או לרשויות החוק אם נדרש.
- 4. אבטחת מידע:
- אנו נוקטים באמצעי אבטחה מתקדמים להגנה על המידע האישי.
- 5. זכויות המשתמש:
- יש לך זכות לעיין במידע, לבקש את תיקונו או מחיקתו.
- 6. יצירת קשר:
- לשאלות, ניתן לפנות אלינו בטלפון 052-229-6269 או בדוא״ל
                shai@clearflow.co.il.
- הצהרת נגישות
- הצהרת נגישות - ClearFlow
- אנו ב-ClearFlow מייחסים חשיבות עליונה להנגשת אתר האינטרנט שלנו. אתר זה הונגש בהתאם להמלצות התקן הישראלי
                (ת״י 5568) לנגישות תכנים באינטרנט ברמת AA, וכן עומד בהנחיות WCAG 2.0.
- פירוט התאמות:
- האתר כולל תמיכה בניווט מקלדת, התאמה לקוראי מסך, ואפשרות להגדלת גופנים
                ושינוי ניגודיות באמצעות תוסף הנגישות (Tabnav) המותקן באתר.
- סייגים:
- למרות מאמצינו להנגיש את כלל דפי האתר, ייתכן ויתגלו חלקים שטרם הונגשו
                במלואם.
- פרטי רכז נגישות:
- במידה ונתקלתם בבעיית נגישות, נשמח לקבל משוב ולתקן זאת בהקדם.
- שם הרכז: שי ישראל
- טלפון: 052-229-6269
- דוא״ל: shai@clearflow.co.il
- בית
- שירותים
- דפי נחיתה
- אוטומציות מתקדמות
- מערכות CRM
- הדרכה והטמעה
- יצירת קשר
- חזרה לעמוד הראשי
- מערכות CRM להשגת שליטה מלאה
- בלב הפועם של עסקים קטנים ועצמאיים, מידע מפוזר הוא כסף אבוד. אנחנו משלבים יכולות בינה מלאכותית אל תוך מערכת הניהול (CRM), והופכים אותה ל'מוח המרכזי' האקטיבי של העסק שלכם – כזה שעובד בשבילכם, בלי שהצוות ישנא לעדכן אותה.
- 5 דקות קריאה
- למה רוב מערכות הניהול ננטשות בעסקים?
- מנכ"לים ועסקים רבים משקיעים כסף רב בקניית מערכות הניהול "המתקדמות ביותר בשוק", רק כדי לגלות בתסכול חודש לאחר מכן שאף אחד במשרד לא באמת משתמש בהן. למה זה קורה? התופעה נקראת 'התנגדות משתמשים'. רוב המערכות הפופולריות מגיעות מהקופסה בארכיטקטורה מורכבת ועמוסה, שמדרישה מהעובד למלא 15 שדות חובה רק כדי לעדכן שיחת טלפון פשוטה. העובדים, שמרגישים שהמערכת רק מעכבת אותם, זונחים אותה באלגנטיות וחוזרים לפתקים על המסך או לאקסלים אישיים במחשב. התוצאה: להנהלה אין 'אמת ארגונית אחת' לקבל דרכה החלטות.
- מיקום לאיור / תרשים (Placeholder)
- הסוד: מינימליזם והתאמת המערכת לצוות
- מערכת ניהול מודרנית לא אמורה להחליף את האינטואיציה האנושית – אלא להגביר אותה ולחסוך מהצוות עבודת כפיים. הדרך היחידה למנוע נטישה היא לגשת לאפיון המערכת 'מלמטה למעלה': לפגוש את צוות השטח ולהבין היכן בדיוק הם נתקעים ביום עבודה עמוס. בעזרת אפיון מדויק, יש לייצר ממשק מינימליסטי שבו רק המידע ההכרחי מוצג. כשהמערכת עושה לעובד חיים קלים יותר (לדוגמה: מתזמנת לבד פולו-אפים ומציגה לו מה בוער הבוקר), האימוץ הארגוני מגיע מעצמו והמידע נשמר באדיקות. מחקרים על ייעול תהליכי עבודה מוכיחים כי תקשורת מדויקת מבוססת-CRM יכולה להפוך לידים למשפכים שממירים בעד 20% יותר, פשוט כי המסר הנכון מגיע ללקוח הנכון, בזמן.
- מיקום לאיור / תרשים (Placeholder)
- בשטח: כשהמידע זורם בטבעיות
- המעבר מרישום ביומן ששוכחים לעדכן, ללוח עבודה דינמי חי ונושם. ברגע שסוכן המכירות שלכם גורר כרטיס לקוח לעמודת 'נשלחה הצעת מחיר', המערכת לא רק מעדכנת טקסט לסטטוס למנהלים, אלא עובדת עבורו: היא תוכל לחולל אוטומטית מסמך רשמי, לתזמן משימת פולו-אפ מוקפדת לעוד 48 שעות בדיוק, ולהכין צפי הכנסות מדורג שיתעדכן קבוע למחלקת הכספים.
- השורה התחתונה
- ניהול עסקאות הרמטי – אין יותר לידים ש'נופלים בין הכיסאות' באמצע תהליך.
- שליטה מוחלטת במידע: הכל מרוכז במקום אחד לקבלת החלטות מהירות ומבוססות רציונל.
- תשתית יציבה המאפשרת קליטת עובדים וחפיפתם באופן טבעי וברור.
- בואו נבנה CRM שהצוות אשכרה ישתמש בו.
- נשמח לשבת איתכם פנים אל פנים, לאפיין נתיב ברור ולהקים סביבת עבודה שתחזיר את האחיזה המלאה בהגה.
- לייעוץ ותכנון חינם
- © 2024 ClearFlow Automation. כל הזכויות שמורות.
- פרטיות
- הצהרת נגישות
- // Webhook Submission Logic
        async function submitToWebhook(payload, submitBtn, errorEl) {
            const webhookUrl = 'https://hook.eu1.make.com/p3evkp5wlspqvo3g1ln8qyxanf23h7as';
            const originalText = submitBtn.innerText;
            try {
                submitBtn.disabled = true;
                submitBtn.innerText = 'שולח...';
                if (errorEl) errorEl.classList.add('hidden');
                const response = await fetch(webhookUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                if (response.ok) return true;
                else throw new Error('Server error');
            } catch (error) {
                console.error('Submission error:', error);
                if (errorEl) {
                    errorEl.innerText = 'חלה שגיאה בשליחה. אנא נסו שנית או פנו אלינו בוואטסאפ.';
                    errorEl.classList.remove('hidden');
                }
                submitBtn.disabled = false;
                submitBtn.innerText = originalText;
                return false;
            }
        }

        // Header Scroll Interaction
        const header = document.getElementById('mainHeader');
        if (header) {
            window.addEventListener('scroll', () => {
                if (window.scrollY > 20) {
                    header.classList.remove('shadow-lg', 'py-4');
                    header.classList.add('shadow-md', 'py-3');
                } else {
                    header.classList.add('shadow-lg', 'py-4');
                    header.classList.remove('shadow-md', 'py-3');
                }
            });
        }

        // Shared Form Validator
        function validateForm(name, phone, privacyChecked) {
            if (!name || !phone) return 'אנא מלאו את כל השדות.';
            if (!privacyChecked) return 'יש לאשר את מדיניות הפרטיות.';
            return null;
        }

        // Hero Form Logic
        const heroForm = document.querySelector('section:first-of-type form');
        if (heroForm) {
            heroForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = heroForm.querySelector('input[name="name"]').value.trim();
                const phoneVal = heroForm.querySelector('input[name="phone"]').value.trim();
                const privacyChecked = heroForm.querySelector('#privacy-hero').checked;
                const errorEl = heroForm.querySelector('.hero-error-message');
                const errMsg = validateForm(nameVal, phoneVal, privacyChecked);
                if (errMsg) {
                    errorEl.innerText = errMsg;
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = heroForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, phone: phoneVal, source: sourceVal }, heroForm.querySelector('button'), errorEl);
                if (success) {
                    document.getElementById('heroFormInputs').classList.add('hidden');
                    document.getElementById('heroFormSuccess').classList.remove('hidden');
                }
            });
        }

        // Footer Form Logic
        const footerForm = document.getElementById('footerForm');
        if (footerForm) {
            footerForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = footerForm.querySelector('input[name="name"]').value.trim();
                const phoneVal = footerForm.querySelector('input[name="phone"]').value.trim();
                const emailVal = footerForm.querySelector('input[name="email"]').value.trim();
                const privacyChecked = document.getElementById('privacy-footer').checked;
                const errorEl = footerForm.querySelector('.footer-error-message');
                if (!nameVal || !phoneVal || !emailVal) {
                    errorEl.innerText = 'אנא מלאו שדות חובה.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                if (!privacyChecked) {
                    errorEl.innerText = 'יש לאשר את מדיניות הפרטיות.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = footerForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, phone: phoneVal, email: emailVal, source: sourceVal }, footerForm.querySelector('button'), errorEl);
                if (success) {
                    footerForm.classList.add('hidden');
                    document.getElementById('footerSuccess').classList.remove('hidden');
                }
            });
        }

        // Lead Magnet Form Logic
        const leadForm = document.getElementById('leadMagnetForm');
        if (leadForm) {
            leadForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = leadForm.querySelector('input[name="fullName"]').value.trim();
                const emailVal = leadForm.querySelector('input[name="email"]').value.trim();
                const privacyChecked = leadForm.querySelector('input[name="privacyAgreement"]').checked;
                const errorEl = leadForm.querySelector('.lead-error-message');
                if (!nameVal || !emailVal) {
                    errorEl.innerText = 'אנא מלאו שם ודוא״ל.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                if (!privacyChecked) {
                    errorEl.innerText = 'יש לאשר את מדיניות הפרטיות.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = leadForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, email: emailVal, source: sourceVal }, leadForm.querySelector('button'), errorEl);
                if (success) {
                    leadForm.classList.add('hidden');
                    document.getElementById('leadMagnetSuccess').classList.remove('hidden');
                }
            });
        }

        // Reveal Animations
        document.addEventListener("DOMContentLoaded", () => {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('is-revealed');
                    }
                });
            }, { threshold: 0.15 });
            document.querySelectorAll('.reveal-element').forEach(el => observer.observe(el));
        });
# public/services/landing-pages.html
- דפי נחיתה שממירים יותר | ClearFlow
- {
        "@context": "https://schema.org",
        "@type": "ProfessionalService",
        "name": "ClearFlow",
        "url": "https://clearflow.co.il/",
        "logo": "https://clearflow.co.il/logo.png",
        "image": "https://clearflow.co.il/og-image.jpg",
        "description": "סוכנות אוטומציה עסקית בישראל. אנו מתמחים בהקמת תהליכי אוטומציה, ניווט נתונים, אינטגרציה בין מערכות (Make.com, Zapier) והטמעת מערכות CRM.",
        "address": {
            "@type": "PostalAddress",
            "addressCountry": "IL"
        },
        "telephone": "+972522296269",
        "email": "shai@clearflow.co.il",
        "founder": {
            "@type": "Person",
            "name": "Shai Israel"
        },
        "areaServed": "IL",
        "priceRange": "$$",
        "knowsAbout": ["Business Automation", "Make.com", "Zapier", "CRM", "Data Integration"]
    }
- דלג לתוכן המרכזי
- פתרונות נגישות לאתרי אינטרנט לפי התקן הישראלי 5568
- הנגשת
            אתרים
- (נפתח בחלון חדש)
- אנו משתמשים בעוגיות (Cookies) כדי להעניק לך את החוויה הטובה ביותר. בהמשך הגלישה, הנך מסכים/ה לשימוש בהן.
                לקריאה נוספת:
- מדיניות הפרטיות
- שלנו.
- הבנתי תודה
- מדיניות פרטיות
- מדיניות פרטיות - ClearFlow
- חברת ClearFlow ('אנחנו', 'אותנו' או 'שלנו') מכבדת את פרטיותך ומחויבת להגנה על מידע אישי שאתה משתף איתנו.
                מדיניות זו מסבירה כיצד אנו אוספים ומשתמשים במידע.
- 1. המידע שאנו אוספים:
- אנו עשויים לאסוף שם מלא, כתובת דוא״ל, מספר טלפון, ומידע על השימוש
                באתר הנאסף אוטומטית באמצעות עוגיות (Cookies).
- 2. מטרות השימוש במידע:
- מתן שירותים, יצירת קשר, תמיכה, ושליחת חומרים שיווקיים (בכפוף
                להסכמתך).
- 3. שיתוף מידע:
- אנו לא נמכור או נשכיר מידע אישי לצדדים שלישיים. מידע עשוי להיות מועבר
                לספקי שירותים חיצוניים לצורך תפעול האתר בלבד, או לרשויות החוק אם נדרש.
- 4. אבטחת מידע:
- אנו נוקטים באמצעי אבטחה מתקדמים להגנה על המידע האישי.
- 5. זכויות המשתמש:
- יש לך זכות לעיין במידע, לבקש את תיקונו או מחיקתו.
- 6. יצירת קשר:
- לשאלות, ניתן לפנות אלינו בטלפון 052-229-6269 או בדוא״ל
                shai@clearflow.co.il.
- הצהרת נגישות
- הצהרת נגישות - ClearFlow
- אנו ב-ClearFlow מייחסים חשיבות עליונה להנגשת אתר האינטרנט שלנו. אתר זה הונגש בהתאם להמלצות התקן הישראלי
                (ת״י 5568) לנגישות תכנים באינטרנט ברמת AA, וכן עומד בהנחיות WCAG 2.0.
- פירוט התאמות:
- האתר כולל תמיכה בניווט מקלדת, התאמה לקוראי מסך, ואפשרות להגדלת גופנים
                ושינוי ניגודיות באמצעות תוסף הנגישות (Tabnav) המותקן באתר.
- סייגים:
- למרות מאמצינו להנגיש את כלל דפי האתר, ייתכן ויתגלו חלקים שטרם הונגשו
                במלואם.
- פרטי רכז נגישות:
- במידה ונתקלתם בבעיית נגישות, נשמח לקבל משוב ולתקן זאת בהקדם.
- שם הרכז: שי ישראל
- טלפון: 052-229-6269
- דוא״ל: shai@clearflow.co.il
- בית
- שירותים
- דפי נחיתה
- אוטומציות מתקדמות
- מערכות CRM
- הדרכה והטמעה
- יצירת קשר
- חזרה לעמוד הראשי
- דפי נחיתה שממירים יותר
- עבור החברות שאנחנו מלווים, תקציב השיווק חייב להצדיק את עצמו. תפסיקו להזרים תנועה יקרה לדפים שלא ממירים. אנחנו מקימים עמודי נחיתה מהירים, מדויקים ומושכי לידים שמתוכננים פסיכולוגית וטכנולוגית להפוך מתעניינים ללקוחות משלמים.
- 3 דקות קריאה
- למה רוב דפי הנחיתה נכשלים?
- משווקים ועסקים רבים מוציאים אלפי שקלים על קמפיינים ממומנים ברשתות, אבל מגלים שהגולשים מגיעים לדף ונוטשים מיד. הסיבה המרכזית לכך היא 'עומס קוגניטיבי'. כשגולש נוחת בדף עמוס בטקסט, בעיצוב מבולגן או ללא מסר ברור ומיידי ההבטחה השיווקית – המוח האנושי בוחר בדרך הקלה ביותר: לסגור את החלון. מעבר לכך, זמני טעינה איטיים וחוסר התאמה מושלמת למובייל הופכים את אחוז הנטישה לכמעט ודאי, מה ששורף את תקציב השיווק שלכם על קליקים ריקים.
- מיקום לאיור / תרשים (Placeholder)
- איך עושים את זה נכון? הפסיכולוגיה של ההמרה
- כדי להפוך מבקר לליד פוטנציאלי, הדף חייב להיות ממוקד כירורגית. זה אומר: קודם כל, כותרת ראשית חזקה וברורה שמסבירה בדיוק איזה כאב אתם פותרים, בלי סיסמאות ריקות. שנית, עיצוב נקי שמכווין את העין של הגולש ישירות אל הפעולה המרכזית (כפתור יצירת קשר או טופס), ללא הסחות דעת, תפריטי ניווט עמוסים או קישורים חיצוניים מיותרים שמוציאים אותו החוצה. לבסוף, הדף חייב להיטען במהירות שיא. מחקרים מאשרים כי עיכוב קליל של שנייה אחת בטעינת האתר חותך את אחוז ההמרה ב-7%. המהירות היא קריטית כדי לשדר אמינות מיידית דרך עדויות ענייניות ועיצוב שמשדר פרמיום.
- מיקום לאיור / תרשים (Placeholder)
- בשטח: התוצאה של דף מדויק
- במקום להביא גולשים לאתר כללי ומפוזר, אנחנו מפנים אותם לדף ממוקד שמתקשר ישירות את 'השורה התחתונה' שלכם. גישה זו מוכיחה את עצמה: עלויות הפרסום שלכם מנוצלות במלואן, מחיר הפנייה הממוצע יורד משמעותית, וכמות הלידים הנכנסים קופצת באותו תקציב פרסום בדיוק.
- השורה התחתונה
- הוזלה משמעותית של עלות הרכישה ללקוח בזכות אחוזי המרה גבוהים יותר.
- חוויית משתמש פרימיום שמשדרת אמינות וממצבת אתכם מעל השוק.
- לכידת מידע (לידים) זורמת ישירות ל-CRM ולצוות המכירות אוטומטית.
- כבר הבאתם את הלקוח עד לדלת. אל תתנו לו לברוח.
- בואו נאפיין יחד דף נחיתה מדויק שיגרום ללקוחות להבין מיד שאתם הפתרון הנכון עבורם ולהשאיר פרטים.
- לייעוץ ותכנון חינם
- © 2024 ClearFlow Automation. כל הזכויות שמורות.
- פרטיות
- הצהרת נגישות
- // Webhook Submission Logic
        async function submitToWebhook(payload, submitBtn, errorEl) {
            const webhookUrl = 'https://hook.eu1.make.com/p3evkp5wlspqvo3g1ln8qyxanf23h7as';
            const originalText = submitBtn.innerText;
            try {
                submitBtn.disabled = true;
                submitBtn.innerText = 'שולח...';
                if (errorEl) errorEl.classList.add('hidden');
                const response = await fetch(webhookUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                if (response.ok) return true;
                else throw new Error('Server error');
            } catch (error) {
                console.error('Submission error:', error);
                if (errorEl) {
                    errorEl.innerText = 'חלה שגיאה בשליחה. אנא נסו שנית או פנו אלינו בוואטסאפ.';
                    errorEl.classList.remove('hidden');
                }
                submitBtn.disabled = false;
                submitBtn.innerText = originalText;
                return false;
            }
        }

        // Header Scroll Interaction
        const header = document.getElementById('mainHeader');
        if (header) {
            window.addEventListener('scroll', () => {
                if (window.scrollY > 20) {
                    header.classList.remove('shadow-lg', 'py-4');
                    header.classList.add('shadow-md', 'py-3');
                } else {
                    header.classList.add('shadow-lg', 'py-4');
                    header.classList.remove('shadow-md', 'py-3');
                }
            });
        }

        // Shared Form Validator
        function validateForm(name, phone, privacyChecked) {
            if (!name || !phone) return 'אנא מלאו את כל השדות.';
            if (!privacyChecked) return 'יש לאשר את מדיניות הפרטיות.';
            return null;
        }

        // Hero Form Logic
        const heroForm = document.querySelector('section:first-of-type form');
        if (heroForm) {
            heroForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = heroForm.querySelector('input[name="name"]').value.trim();
                const phoneVal = heroForm.querySelector('input[name="phone"]').value.trim();
                const privacyChecked = heroForm.querySelector('#privacy-hero').checked;
                const errorEl = heroForm.querySelector('.hero-error-message');
                const errMsg = validateForm(nameVal, phoneVal, privacyChecked);
                if (errMsg) {
                    errorEl.innerText = errMsg;
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = heroForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, phone: phoneVal, source: sourceVal }, heroForm.querySelector('button'), errorEl);
                if (success) {
                    document.getElementById('heroFormInputs').classList.add('hidden');
                    document.getElementById('heroFormSuccess').classList.remove('hidden');
                }
            });
        }

        // Footer Form Logic
        const footerForm = document.getElementById('footerForm');
        if (footerForm) {
            footerForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = footerForm.querySelector('input[name="name"]').value.trim();
                const phoneVal = footerForm.querySelector('input[name="phone"]').value.trim();
                const emailVal = footerForm.querySelector('input[name="email"]').value.trim();
                const privacyChecked = document.getElementById('privacy-footer').checked;
                const errorEl = footerForm.querySelector('.footer-error-message');
                if (!nameVal || !phoneVal || !emailVal) {
                    errorEl.innerText = 'אנא מלאו שדות חובה.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                if (!privacyChecked) {
                    errorEl.innerText = 'יש לאשר את מדיניות הפרטיות.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = footerForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, phone: phoneVal, email: emailVal, source: sourceVal }, footerForm.querySelector('button'), errorEl);
                if (success) {
                    footerForm.classList.add('hidden');
                    document.getElementById('footerSuccess').classList.remove('hidden');
                }
            });
        }

        // Lead Magnet Form Logic
        const leadForm = document.getElementById('leadMagnetForm');
        if (leadForm) {
            leadForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = leadForm.querySelector('input[name="fullName"]').value.trim();
                const emailVal = leadForm.querySelector('input[name="email"]').value.trim();
                const privacyChecked = leadForm.querySelector('input[name="privacyAgreement"]').checked;
                const errorEl = leadForm.querySelector('.lead-error-message');
                if (!nameVal || !emailVal) {
                    errorEl.innerText = 'אנא מלאו שם ודוא״ל.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                if (!privacyChecked) {
                    errorEl.innerText = 'יש לאשר את מדיניות הפרטיות.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = leadForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, email: emailVal, source: sourceVal }, leadForm.querySelector('button'), errorEl);
                if (success) {
                    leadForm.classList.add('hidden');
                    document.getElementById('leadMagnetSuccess').classList.remove('hidden');
                }
            });
        }

        // Reveal Animations
        document.addEventListener("DOMContentLoaded", () => {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('is-revealed');
                    }
                });
            }, { threshold: 0.15 });
            document.querySelectorAll('.reveal-element').forEach(el => observer.observe(el));
        });
# public/services/training.html
- ליווי והדרכת הצוות שלכם | ClearFlow
- {
        "@context": "https://schema.org",
        "@type": "ProfessionalService",
        "name": "ClearFlow",
        "url": "https://clearflow.co.il/",
        "logo": "https://clearflow.co.il/logo.png",
        "image": "https://clearflow.co.il/og-image.jpg",
        "description": "סוכנות אוטומציה עסקית בישראל. אנו מתמחים בהקמת תהליכי אוטומציה, ניווט נתונים, אינטגרציה בין מערכות (Make.com, Zapier) והטמעת מערכות CRM.",
        "address": {
            "@type": "PostalAddress",
            "addressCountry": "IL"
        },
        "telephone": "+972522296269",
        "email": "shai@clearflow.co.il",
        "founder": {
            "@type": "Person",
            "name": "Shai Israel"
        },
        "areaServed": "IL",
        "priceRange": "$$",
        "knowsAbout": ["Business Automation", "Make.com", "Zapier", "CRM", "Data Integration"]
    }
- דלג לתוכן המרכזי
- פתרונות נגישות לאתרי אינטרנט לפי התקן הישראלי 5568
- הנגשת
            אתרים
- (נפתח בחלון חדש)
- אנו משתמשים בעוגיות (Cookies) כדי להעניק לך את החוויה הטובה ביותר. בהמשך הגלישה, הנך מסכים/ה לשימוש בהן.
                לקריאה נוספת:
- מדיניות הפרטיות
- שלנו.
- הבנתי תודה
- מדיניות פרטיות
- מדיניות פרטיות - ClearFlow
- חברת ClearFlow ('אנחנו', 'אותנו' או 'שלנו') מכבדת את פרטיותך ומחויבת להגנה על מידע אישי שאתה משתף איתנו.
                מדיניות זו מסבירה כיצד אנו אוספים ומשתמשים במידע.
- 1. המידע שאנו אוספים:
- אנו עשויים לאסוף שם מלא, כתובת דוא״ל, מספר טלפון, ומידע על השימוש
                באתר הנאסף אוטומטית באמצעות עוגיות (Cookies).
- 2. מטרות השימוש במידע:
- מתן שירותים, יצירת קשר, תמיכה, ושליחת חומרים שיווקיים (בכפוף
                להסכמתך).
- 3. שיתוף מידע:
- אנו לא נמכור או נשכיר מידע אישי לצדדים שלישיים. מידע עשוי להיות מועבר
                לספקי שירותים חיצוניים לצורך תפעול האתר בלבד, או לרשויות החוק אם נדרש.
- 4. אבטחת מידע:
- אנו נוקטים באמצעי אבטחה מתקדמים להגנה על המידע האישי.
- 5. זכויות המשתמש:
- יש לך זכות לעיין במידע, לבקש את תיקונו או מחיקתו.
- 6. יצירת קשר:
- לשאלות, ניתן לפנות אלינו בטלפון 052-229-6269 או בדוא״ל
                shai@clearflow.co.il.
- הצהרת נגישות
- הצהרת נגישות - ClearFlow
- אנו ב-ClearFlow מייחסים חשיבות עליונה להנגשת אתר האינטרנט שלנו. אתר זה הונגש בהתאם להמלצות התקן הישראלי
                (ת״י 5568) לנגישות תכנים באינטרנט ברמת AA, וכן עומד בהנחיות WCAG 2.0.
- פירוט התאמות:
- האתר כולל תמיכה בניווט מקלדת, התאמה לקוראי מסך, ואפשרות להגדלת גופנים
                ושינוי ניגודיות באמצעות תוסף הנגישות (Tabnav) המותקן באתר.
- סייגים:
- למרות מאמצינו להנגיש את כלל דפי האתר, ייתכן ויתגלו חלקים שטרם הונגשו
                במלואם.
- פרטי רכז נגישות:
- במידה ונתקלתם בבעיית נגישות, נשמח לקבל משוב ולתקן זאת בהקדם.
- שם הרכז: שי ישראל
- טלפון: 052-229-6269
- דוא״ל: shai@clearflow.co.il
- בית
- שירותים
- דפי נחיתה
- אוטומציות מתקדמות
- מערכות CRM
- הדרכה והטמעה
- יצירת קשר
- חזרה לעמוד הראשי
- ליווי צמוד והדרכת חפיפה (Onboarding)
- במציאות של חברות וארגונים צומחים, מערכת שהצוות לא משתמש בה היא פשוט יקרה ומיותרת. אנחנו כאן כדי להבטיח שהטכנולוגיה החדשה שלכם באמת תשרת את האנשים - באמצעות הדרכה מקצועית, ליווי אישי אנושי והטמעה בגובה העיניים.
- 3 דקות קריאה
- המרד השקט: כשהטכנולוגיה מפחידה צוותים ותיקים
- אחת הבעיות היקרות ביותר בתהליכי שדרוג טכנולוגי בארגון היא רגע ההשקה ושבוע הקליטה שאחריו. בעלי עסקים יכולים לרכוש את תוכנות הפרמיום המבריקות בעולם ולהוציא הון על פיתוח, אבל המערכת בסוף מופעלת על ידי עובדים בשר ודם. כשהטכנולוגיה 'נוחתת' על יושבי המשרד בפתאומיות ובלי שקיפות או תיווך בגובה העיניים, נוצר מצב של 'מרד שקט'. העובדים עלולים להרגיש שהתוכנה מסובכת מדי או אפילו מאיימת להחליף אותם, ומנסים בעקשנות למצוא דרכים עוקפות אליה, לרוב בחזרה צורמת לשיטות הידניות המערכבות של פעם.
- מיקום לאיור / תרשים (Placeholder)
- הסוד להטמעה שמצליחה: בני אדם, לא רק קוד
- טרנספורמציה דיגיטלית אמיתית מתחילה בצוות שלכם. אנחנו ב-ClearFlow מבינים ששום מערכת עטורת פרסים אינה שווה הרבה אם הצוות לא יודע, או לא רוצה, להשתמש בה. לכן, אנחנו לא "זורקים המפתחות" והולכים עם סיום הפיתוח. אנחנו מספקים הדרכות חיות, ליווי ותמיכה צמודים לכל צוות לפי הצרכים שלו – כדי להבטיח מעבר חלק, ללא חששות וללא תקלות, עד שאחרון העובדים ירגיש בנוח עם סביבת העבודה החדשה.
- מיקום לאיור / תרשים (Placeholder)
- בשטח: איך מעבירים ארגון מסודר בהצלחה?
- התהליך שנקבע לעסק שלכם משלב הדרכות מקצועיות בגובה העיניים, פגישות ייעוץ לתהליכים פנימיים וזמינות לתמיכה שוטפת שיבטיחו הטמעה אפקטיבית. אם מישהו מהצוות שלכם מסתבך קלות עם פעולה פשוטה במערכת ה-CRM, אנו נוודא שהוא מקבל פתרון אנושי וברור – ולא ישאיר אותו לבזבז זמן.
- השורה התחתונה
- שימוש בטוח במערכות: העובדים שלכם רוכשים ביטחון ושולטים בטכנולוגיה במקום להימנע ממנה.
- הדרכה אישית ומעשית: מסגרת חפיפה ברורה ואנושית שמאפשרת קליטה קלה של כלים בארגון.
- מקסימום החזר השקעה (ROI) מהכלים הדיגיטליים שלכם – פשוט כי באמת משתמשים בהם.
- בואו נעניק לצוות שלכם את הביטחון שהוא צריך.
- אל תשאירו את הכלים היקרים שלכם להעלות אבק. נכנסים למערכת חדשה? דברו איתנו כדי להכניס את הארגון לדרך המלך הטכנולוגית בצורה חלקה.
- לייעוץ ותכנון חינם
- © 2024 ClearFlow Automation. כל הזכויות שמורות.
- פרטיות
- הצהרת נגישות
- // Webhook Submission Logic
        async function submitToWebhook(payload, submitBtn, errorEl) {
            const webhookUrl = 'https://hook.eu1.make.com/p3evkp5wlspqvo3g1ln8qyxanf23h7as';
            const originalText = submitBtn.innerText;
            try {
                submitBtn.disabled = true;
                submitBtn.innerText = 'שולח...';
                if (errorEl) errorEl.classList.add('hidden');
                const response = await fetch(webhookUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                if (response.ok) return true;
                else throw new Error('Server error');
            } catch (error) {
                console.error('Submission error:', error);
                if (errorEl) {
                    errorEl.innerText = 'חלה שגיאה בשליחה. אנא נסו שנית או פנו אלינו בוואטסאפ.';
                    errorEl.classList.remove('hidden');
                }
                submitBtn.disabled = false;
                submitBtn.innerText = originalText;
                return false;
            }
        }

        // Header Scroll Interaction
        const header = document.getElementById('mainHeader');
        if (header) {
            window.addEventListener('scroll', () => {
                if (window.scrollY > 20) {
                    header.classList.remove('shadow-lg', 'py-4');
                    header.classList.add('shadow-md', 'py-3');
                } else {
                    header.classList.add('shadow-lg', 'py-4');
                    header.classList.remove('shadow-md', 'py-3');
                }
            });
        }

        // Shared Form Validator
        function validateForm(name, phone, privacyChecked) {
            if (!name || !phone) return 'אנא מלאו את כל השדות.';
            if (!privacyChecked) return 'יש לאשר את מדיניות הפרטיות.';
            return null;
        }

        // Hero Form Logic
        const heroForm = document.querySelector('section:first-of-type form');
        if (heroForm) {
            heroForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = heroForm.querySelector('input[name="name"]').value.trim();
                const phoneVal = heroForm.querySelector('input[name="phone"]').value.trim();
                const privacyChecked = heroForm.querySelector('#privacy-hero').checked;
                const errorEl = heroForm.querySelector('.hero-error-message');
                const errMsg = validateForm(nameVal, phoneVal, privacyChecked);
                if (errMsg) {
                    errorEl.innerText = errMsg;
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = heroForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, phone: phoneVal, source: sourceVal }, heroForm.querySelector('button'), errorEl);
                if (success) {
                    document.getElementById('heroFormInputs').classList.add('hidden');
                    document.getElementById('heroFormSuccess').classList.remove('hidden');
                }
            });
        }

        // Footer Form Logic
        const footerForm = document.getElementById('footerForm');
        if (footerForm) {
            footerForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = footerForm.querySelector('input[name="name"]').value.trim();
                const phoneVal = footerForm.querySelector('input[name="phone"]').value.trim();
                const emailVal = footerForm.querySelector('input[name="email"]').value.trim();
                const privacyChecked = document.getElementById('privacy-footer').checked;
                const errorEl = footerForm.querySelector('.footer-error-message');
                if (!nameVal || !phoneVal || !emailVal) {
                    errorEl.innerText = 'אנא מלאו שדות חובה.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                if (!privacyChecked) {
                    errorEl.innerText = 'יש לאשר את מדיניות הפרטיות.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = footerForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, phone: phoneVal, email: emailVal, source: sourceVal }, footerForm.querySelector('button'), errorEl);
                if (success) {
                    footerForm.classList.add('hidden');
                    document.getElementById('footerSuccess').classList.remove('hidden');
                }
            });
        }

        // Lead Magnet Form Logic
        const leadForm = document.getElementById('leadMagnetForm');
        if (leadForm) {
            leadForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const nameVal = leadForm.querySelector('input[name="fullName"]').value.trim();
                const emailVal = leadForm.querySelector('input[name="email"]').value.trim();
                const privacyChecked = leadForm.querySelector('input[name="privacyAgreement"]').checked;
                const errorEl = leadForm.querySelector('.lead-error-message');
                if (!nameVal || !emailVal) {
                    errorEl.innerText = 'אנא מלאו שם ודוא״ל.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                if (!privacyChecked) {
                    errorEl.innerText = 'יש לאשר את מדיניות הפרטיות.';
                    errorEl.classList.remove('hidden');
                    return;
                }
                const sourceVal = leadForm.querySelector('input[name="source"]').value;
                const success = await submitToWebhook({ fullName: nameVal, email: emailVal, source: sourceVal }, leadForm.querySelector('button'), errorEl);
                if (success) {
                    leadForm.classList.add('hidden');
                    document.getElementById('leadMagnetSuccess').classList.remove('hidden');
                }
            });
        }

        // Reveal Animations
        document.addEventListener("DOMContentLoaded", () => {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('is-revealed');
                    }
                });
            }, { threshold: 0.15 });
            document.querySelectorAll('.reveal-element').forEach(el => observer.observe(el));
        });
